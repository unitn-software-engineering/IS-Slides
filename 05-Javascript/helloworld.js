// console.log("Hello", "World!");

// const myArray = [1, 2, 3, 4, 5];
// myArray.forEach( v => console.log(v) );


// function Car(type, model, color) {
//     this.type = type;
//     this.model = model;
//     this.color = color;
//     this.description = function() {
//         return this.color + ", " + this.model + ", " + this.type;
//     };
// }
// const myCar = {}
// Car.bind(myCar)('Fiat', '500', 'red');
// console.log(myCar.color);



// class Car3 {
//     type = 'default';

//     constructor(type, model, color) {
//         this.type = type;
//         this.model = model;
//         this.color = color;
//     }

//     description() {
//         return this.color + ", " + this.model + ", " + this.type;
//     };
// }

// new Car3('Fiat', '500', 'red').wheels = 4;




/** @type {Array<string>} */
const names = [];

/** @type {function(a:string, string):integer} */
const myFn =  ( a, b ) => { return a + b; }

/**
* Represents a book.
* @param {string} title - The title of the book.
* @returns {Book}
*/
function Book(title) {
    title.
}
/** @type {function(string, string)} */
( a, b ) => { return a + b; }